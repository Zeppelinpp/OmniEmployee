//! UI components for OmniEmployee GUI
//!
//! These modules extend the `App` struct with rendering methods.
//! They are imported for their side effects (impl blocks).

mod header;
mod input;
mod messages;
mod sidebar;
